info:
  name: /api/v1/packages
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/packages/nwks/ns1/pack"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
